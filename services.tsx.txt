import { createFileRoute } from "@tanstack/react-router";
import { Check, MessageCircle, Sparkles } from "lucide-react";
import { SiteLayout } from "@/components/SiteLayout";
import { PageHeader } from "@/components/PageHeader";
import { Reveal } from "@/components/Reveal";
import { serviceCategories, site } from "@/lib/site";
import bannerImg from "@/assets/services-banner.jpg";

export const Route = createFileRoute("/services")({
  head: () => ({
    meta: [
      { title: "Services | Ivy's Beauty Secret Ladies Salon & Spa, Abu Dhabi" },
      {
        name: "description",
        content:
          "Explore our full menu: hair, makeup, facials, nails, brows & lashes, waxing and spa treatments at Ivy's Beauty Secret in Musaffah, Abu Dhabi.",
      },
      {
        property: "og:title",
        content: "Services | Ivy's Beauty Secret Ladies Salon & Spa",
      },
      {
        property: "og:description",
        content:
          "Premium hair, makeup, facials, nails, waxing and spa treatments in Abu Dhabi.",
      },
    ],
  }),
  component: Services,
});

function Services() {
  return (
    <SiteLayout>
      <PageHeader
        eyebrow="Our Menu"
        title="Treatments & Services"
        image={bannerImg}
        subtitle="A complete collection of premium beauty and wellness rituals — each designed to leave you glowing, confident and beautifully cared for."
      >
        <a
          href={site.whatsappMessage}
          target="_blank"
          rel="noopener noreferrer"
          className="inline-flex items-center gap-2 rounded-full bg-gradient-rose px-7 py-3.5 text-sm font-medium text-primary-foreground shadow-soft transition-transform duration-300 hover:scale-105"
        >
          <MessageCircle className="h-4 w-4" /> Enquire & Book
        </a>
      </PageHeader>

      <section className="px-6 py-20 sm:px-8">
        <div className="mx-auto max-w-5xl space-y-16">
          {serviceCategories.map((cat, i) => (
            <Reveal key={cat.id} delay={(i % 3) * 80}>
              <div id={cat.id} className="scroll-mt-28">
                <div className="flex flex-col gap-1 border-b border-border pb-5">
                  <span className="inline-flex items-center gap-2 text-xs font-medium uppercase tracking-luxe text-primary">
                    <Sparkles className="h-3.5 w-3.5 text-gold" /> {`0${i + 1}`}
                  </span>
                  <h2 className="font-display text-3xl text-foreground sm:text-4xl">
                    {cat.title}
                  </h2>
                  <p className="mt-1 max-w-2xl text-sm leading-relaxed text-muted-foreground">
                    {cat.blurb}
                  </p>
                </div>
                <ul className="mt-6 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">
                  {cat.services.map((s) => (
                    <li
                      key={s}
                      className="flex items-center gap-3 rounded-xl border border-border bg-card px-4 py-3 text-sm text-foreground transition-colors hover:border-primary/40"
                    >
                      <span className="flex h-6 w-6 shrink-0 items-center justify-center rounded-full bg-secondary text-primary">
                        <Check className="h-3.5 w-3.5" />
                      </span>
                      {s}
                    </li>
                  ))}
                </ul>
              </div>
            </Reveal>
          ))}
        </div>

        <Reveal>
          <div className="mx-auto mt-20 max-w-3xl rounded-3xl border border-border bg-gradient-soft p-10 text-center shadow-soft">
            <h3 className="font-display text-3xl text-foreground">
              Not Sure Which Treatment to Choose?
            </h3>
            <p className="mx-auto mt-3 max-w-xl text-sm leading-relaxed text-muted-foreground">
              Our beauty experts are happy to recommend the perfect service for you.
              Message us and we'll guide you every step of the way.
            </p>
            <a
              href={site.whatsappMessage}
              target="_blank"
              rel="noopener noreferrer"
              className="mt-7 inline-flex items-center gap-2 rounded-full bg-gradient-rose px-7 py-3.5 text-sm font-medium text-primary-foreground shadow-soft transition-transform duration-300 hover:scale-105"
            >
              <MessageCircle className="h-4 w-4" /> Chat with Our Team
            </a>
          </div>
        </Reveal>
      </section>
    </SiteLayout>
  );
}
