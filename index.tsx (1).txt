import { createFileRoute, Link } from "@tanstack/react-router";
import {
  Sparkles,
  MessageCircle,
  ArrowRight,
  Crown,
  Heart,
  ShieldCheck,
  Star,
  MapPin,
  Scissors,
  Brush,
  Hand,
  Flower2,
  Gem,
} from "lucide-react";
import { SiteLayout } from "@/components/SiteLayout";
import { Reveal } from "@/components/Reveal";
import { SectionHeading } from "@/components/SectionHeading";
import { serviceCategories, site } from "@/lib/site";
import heroModel from "@/assets/hero-model.jpg";
import interiorImg from "@/assets/salon-interior.jpg";
import hairImg from "@/assets/service-hair.jpg";
import nailsImg from "@/assets/service-nails.jpg";
import makeupImg from "@/assets/service-makeup.jpg";
import facialImg from "@/assets/service-facial.jpg";

export const Route = createFileRoute("/")({
  head: () => ({
    meta: [
      { title: "Ivy's Beauty Secret | Premium Ladies Salon & Spa in Abu Dhabi" },
      {
        name: "description",
        content:
          "Experience luxury beauty at Ivy's Beauty Secret — premium hair, makeup, facials, nails & spa in Musaffah, Abu Dhabi. Be a Pampered Princess.",
      },
    ],
  }),
  component: Home,
});

const serviceImages: Record<string, string> = {
  hair: hairImg,
  makeup: makeupImg,
  facials: facialImg,
  nails: nailsImg,
};

const categoryIcons: Record<string, typeof Scissors> = {
  hair: Scissors,
  makeup: Brush,
  facials: Sparkles,
  nails: Hand,
  brows: Star,
  waxing: Flower2,
  wellness: Heart,
};

const pillars = [
  {
    icon: Crown,
    title: "Luxury, Not Budget",
    text: "Every detail — from our interiors to our products — is curated to feel premium, calming and indulgent.",
  },
  {
    icon: ShieldCheck,
    title: "Expert & Hygienic",
    text: "Skilled, certified beauty professionals delivering safe, meticulous treatments in a spotless space.",
  },
  {
    icon: Heart,
    title: "Made for Women",
    text: "A private ladies-only sanctuary where you can fully relax, refresh and feel beautifully you.",
  },
];

const testimonials = [
  {
    name: "Aisha M.",
    text: "The most luxurious salon experience in Abu Dhabi. My bridal makeup was flawless and lasted all day.",
  },
  {
    name: "Fatima K.",
    text: "Stunning balayage and the calmest spa atmosphere. I always leave feeling like a princess.",
  },
  {
    name: "Noor S.",
    text: "Hydra facial gave me the glow of my life. The team is professional, warm and so talented.",
  },
];

function Home() {
  return (
    <SiteLayout>
      {/* HERO */}
      <section className="relative flex min-h-screen items-center overflow-hidden">
        {/* Model image */}
        <img
          src={heroModel}
          alt="Elegant woman enjoying a luxury spa experience at Ivy's Beauty Secret"
          className="pointer-events-none absolute inset-y-0 right-0 h-full w-full object-cover object-[68%_center] sm:w-[64%] lg:w-[55%]"
          width={1024}
          height={1280}
          fetchPriority="high"
        />
        {/* Cinematic overlays blending model into the plum canvas */}
        <div className="absolute inset-0 bg-gradient-to-r from-background via-background/90 to-background/20 sm:via-background/70 sm:to-transparent" />
        <div className="absolute inset-0 bg-gradient-to-t from-background via-background/30 to-background/60" />
        <div className="absolute inset-x-0 bottom-0 h-40 bg-gradient-to-t from-background to-transparent" />

        <div className="relative mx-auto w-full max-w-7xl px-6 pt-28 pb-14 sm:px-8">
          <div className="max-w-2xl">
            <div className="animate-float-up inline-flex items-center gap-2 rounded-full border border-gold/40 bg-charcoal/40 px-4 py-2 text-[0.68rem] font-medium uppercase tracking-luxe text-gold shadow-soft backdrop-blur-md sm:text-xs">
              <Sparkles className="h-3.5 w-3.5" />
              Premium Ladies Salon &amp; Spa · Abu Dhabi
            </div>
            <h1 className="animate-float-up mt-6 font-display text-6xl font-medium leading-[1.02] text-foreground sm:text-7xl" style={{ animationDelay: "120ms" }}>
              Where Beauty
              <br />
              Becomes
              <span className="mt-1 block text-gradient-rose shimmer font-script text-7xl font-normal leading-[1.1] sm:text-8xl">
                an Experience
              </span>
            </h1>
            <p className="animate-float-up mt-6 max-w-md text-base leading-relaxed text-muted-foreground sm:text-lg" style={{ animationDelay: "240ms" }}>
              Step into a sanctuary of elegance and self-care. From radiant skin to
              flawless hair and bridal glamour — discover the secret to feeling like a
              pampered princess.
            </p>
            <div className="animate-float-up mt-9 flex flex-col gap-3 sm:flex-row" style={{ animationDelay: "360ms" }}>
              <a
                href={site.whatsappMessage}
                target="_blank"
                rel="noopener noreferrer"
                className="inline-flex items-center justify-center gap-2 rounded-full bg-gradient-rose px-7 py-3.5 text-sm font-medium text-primary-foreground shadow-luxe ring-1 ring-primary/40 transition-transform duration-300 hover:scale-105"
              >
                <MessageCircle className="h-4 w-4" />
                Book Your Appointment
                <ArrowRight className="h-4 w-4" />
              </a>
              <Link
                to="/services"
                className="inline-flex items-center justify-center gap-2 rounded-full border border-gold/40 bg-charcoal/30 px-7 py-3.5 text-sm font-medium text-foreground backdrop-blur-md transition-colors hover:bg-charcoal/50"
              >
                Explore Services
                <ArrowRight className="h-4 w-4" />
              </Link>
            </div>

            {/* Trust bar */}
            <div className="animate-float-up mt-10 max-w-xl rounded-2xl border border-gold/25 bg-charcoal/40 p-5 shadow-soft backdrop-blur-md" style={{ animationDelay: "480ms" }}>
              <div className="flex flex-col gap-5 sm:flex-row sm:items-center sm:gap-6">
                <div className="shrink-0">
                  <div className="flex items-center gap-1">
                    {[...Array(5)].map((_, i) => (
                      <Star key={i} className="h-4 w-4 fill-gold text-gold" />
                    ))}
                  </div>
                  <p className="mt-1.5 text-sm font-medium text-foreground">5.0 · 500+ Reviews</p>
                  <p className="text-xs text-muted-foreground">Loved by women across Abu Dhabi</p>
                </div>
                <div className="hidden h-12 w-px bg-gold/25 sm:block" />
                <div className="grid flex-1 grid-cols-3 gap-3 text-center">
                  {[
                    { icon: Brush, label: "Expert Stylists" },
                    { icon: Flower2, label: "Premium Products" },
                    { icon: Gem, label: "Luxury Experience" },
                  ].map((f) => (
                    <div key={f.label} className="flex flex-col items-center gap-1.5">
                      <f.icon className="h-5 w-5 text-gold" />
                      <span className="text-[0.7rem] font-medium leading-tight text-foreground/90">
                        {f.label}
                      </span>
                    </div>
                  ))}
                </div>
              </div>
            </div>

            <p className="animate-float-up mt-6 text-center font-script text-2xl text-gold/90 sm:text-left" style={{ animationDelay: "600ms" }}>
              Because you deserve the best
            </p>
          </div>
        </div>
      </section>


      {/* PILLARS */}
      <section className="relative -mt-16 z-10 px-6 sm:px-8">
        <div className="mx-auto grid max-w-6xl gap-5 md:grid-cols-3">
          {pillars.map((p, i) => (
            <Reveal key={p.title} delay={i * 120}>
              <div className="h-full rounded-2xl border border-border bg-card p-7 shadow-soft transition-transform duration-500 hover:-translate-y-1.5">
                <div className="flex h-12 w-12 items-center justify-center rounded-full bg-gradient-rose text-primary-foreground">
                  <p.icon className="h-5 w-5" />
                </div>
                <h3 className="mt-5 font-display text-2xl text-foreground">{p.title}</h3>
                <p className="mt-2 text-sm leading-relaxed text-muted-foreground">{p.text}</p>
              </div>
            </Reveal>
          ))}
        </div>
      </section>

      {/* ABOUT / INTRO */}
      <section className="px-6 py-24 sm:px-8">
        <div className="mx-auto grid max-w-7xl items-center gap-12 lg:grid-cols-2">
          <Reveal>
            <div className="relative">
              <img
                src={interiorImg}
                alt="Elegant interior of Ivy's Beauty Secret salon"
                loading="lazy"
                width={1280}
                height={1024}
                className="rounded-3xl shadow-luxe"
              />
              <div className="absolute -bottom-6 -right-4 hidden rounded-2xl border border-border bg-card/95 p-5 shadow-luxe backdrop-blur sm:block">
                <p className="font-display text-3xl text-primary">7+</p>
                <p className="text-xs uppercase tracking-wide text-muted-foreground">
                  Service Categories
                </p>
              </div>
            </div>
          </Reveal>
          <Reveal delay={150}>
            <span className="inline-flex items-center gap-2 text-xs font-medium uppercase tracking-luxe text-primary">
              <span className="h-px w-6 bg-gold" /> Welcome to Ivy's
            </span>
            <h2 className="mt-4 font-display text-4xl font-medium leading-tight text-foreground sm:text-5xl">
              A Secret Worth Discovering
            </h2>
            <p className="mt-5 text-base leading-relaxed text-muted-foreground">
              Ivy's Beauty Secret is a premium ladies salon & spa in the heart of
              Musaffah, Abu Dhabi — created for women who believe self-care is an art.
              Our philosophy is simple: every guest deserves to feel like a pampered
              princess.
            </p>
            <p className="mt-4 text-base leading-relaxed text-muted-foreground">
              From transformative hair artistry and luminous facials to bridal glamour
              and serene spa rituals, our talented team blends expertise with warmth in a
              space designed entirely around your comfort.
            </p>
            <Link
              to="/about"
              className="mt-7 inline-flex items-center gap-2 text-sm font-medium uppercase tracking-[0.12em] text-primary transition-colors hover:text-foreground"
            >
              Our Story <ArrowRight className="h-4 w-4" />
            </Link>
          </Reveal>
        </div>
      </section>

      {/* SERVICES PREVIEW */}
      <section className="bg-gradient-soft px-6 py-24 sm:px-8">
        <Reveal>
          <SectionHeading
            eyebrow="Our Services"
            title="Curated Beauty Rituals"
            subtitle="A complete menu of premium treatments, thoughtfully organised so you can find exactly the experience you're craving."
          />
        </Reveal>
        <div className="mx-auto mt-14 grid max-w-6xl gap-6 sm:grid-cols-2 lg:grid-cols-4">
          {serviceCategories.slice(0, 4).map((cat, i) => (
            <Reveal key={cat.id} delay={i * 100}>
              <Link
                to="/services"
                hash={cat.id}
                className="group block overflow-hidden rounded-2xl border border-border bg-card shadow-soft transition-all duration-500 hover:-translate-y-1.5 hover:shadow-luxe"
              >
                <div className="relative aspect-[4/5] overflow-hidden">
                  <img
                    src={serviceImages[cat.id]}
                    alt={cat.title}
                    loading="lazy"
                    width={900}
                    height={1100}
                    className="h-full w-full object-cover transition-transform duration-700 group-hover:scale-110"
                  />
                  <div className="absolute inset-0 bg-gradient-to-t from-charcoal/70 to-transparent" />
                  <h3 className="absolute bottom-4 left-5 font-display text-2xl text-white">
                    {cat.title}
                  </h3>
                </div>
                <p className="px-5 py-4 text-sm leading-relaxed text-muted-foreground">
                  {cat.blurb}
                </p>
              </Link>
            </Reveal>
          ))}
        </div>
        <Reveal delay={200}>
          <div className="mt-12 text-center">
            <Link
              to="/services"
              className="inline-flex items-center gap-2 rounded-full bg-gradient-rose px-7 py-3.5 text-sm font-medium text-primary-foreground shadow-soft transition-transform duration-300 hover:scale-105"
            >
              View Full Service Menu <ArrowRight className="h-4 w-4" />
            </Link>
          </div>
        </Reveal>
      </section>

      {/* SERVICE CATEGORY LIST */}
      <section className="px-6 py-24 sm:px-8">
        <Reveal>
          <SectionHeading
            eyebrow="The Full Experience"
            title="Everything You Need to Glow"
          />
        </Reveal>
        <div className="mx-auto mt-12 grid max-w-5xl gap-4 sm:grid-cols-2 lg:grid-cols-3">
          {serviceCategories.map((cat, i) => {
            const Icon = categoryIcons[cat.id] ?? Sparkles;
            return (
              <Reveal key={cat.id} delay={i * 70}>
                <div className="flex h-full items-start gap-4 rounded-2xl border border-border bg-card p-5 transition-colors hover:border-primary/40">
                  <div className="flex h-11 w-11 shrink-0 items-center justify-center rounded-full bg-secondary text-primary">
                    <Icon className="h-5 w-5" />
                  </div>
                  <div>
                    <h3 className="font-display text-xl text-foreground">{cat.title}</h3>
                    <p className="mt-1 text-sm leading-relaxed text-muted-foreground">
                      {cat.blurb}
                    </p>
                  </div>
                </div>
              </Reveal>
            );
          })}
        </div>
      </section>

      {/* TESTIMONIALS */}
      <section className="bg-gradient-soft px-6 py-24 sm:px-8">
        <Reveal>
          <SectionHeading
            eyebrow="Kind Words"
            title="Beloved by Our Guests"
          />
        </Reveal>
        <div className="mx-auto mt-14 grid max-w-6xl gap-6 md:grid-cols-3">
          {testimonials.map((t, i) => (
            <Reveal key={t.name} delay={i * 120}>
              <figure className="flex h-full flex-col rounded-2xl border border-border bg-card p-7 shadow-soft">
                <div className="flex gap-1">
                  {[...Array(5)].map((_, j) => (
                    <Star key={j} className="h-4 w-4 fill-gold text-gold" />
                  ))}
                </div>
                <blockquote className="mt-4 flex-1 font-display text-xl italic leading-relaxed text-foreground">
                  "{t.text}"
                </blockquote>
                <figcaption className="mt-5 text-sm font-medium uppercase tracking-wide text-primary">
                  {t.name}
                </figcaption>
              </figure>
            </Reveal>
          ))}
        </div>
      </section>

      {/* CTA */}
      <section className="px-6 py-24 sm:px-8">
        <Reveal>
          <div className="relative mx-auto max-w-6xl overflow-hidden rounded-[2rem] bg-charcoal px-8 py-16 text-center shadow-luxe sm:px-16 sm:py-20">
            <div className="absolute inset-0 opacity-20 bg-gradient-rose" aria-hidden />
            <div className="relative">
              <Crown className="mx-auto h-10 w-10 text-gold" />
              <h2 className="mt-5 font-display text-4xl font-medium text-white sm:text-5xl">
                Ready to Be Pampered?
              </h2>
              <p className="mx-auto mt-4 max-w-xl text-base leading-relaxed text-white/80">
                Reserve your moment of luxury today. Message us on WhatsApp and our team
                will help you find the perfect appointment.
              </p>
              <div className="mt-8 flex flex-col items-center justify-center gap-3 sm:flex-row">
                <a
                  href={site.whatsappMessage}
                  target="_blank"
                  rel="noopener noreferrer"
                  className="inline-flex items-center gap-2 rounded-full bg-gradient-rose px-7 py-3.5 text-sm font-medium text-primary-foreground shadow-luxe transition-transform duration-300 hover:scale-105"
                >
                  <MessageCircle className="h-4 w-4" /> Book on WhatsApp
                </a>
                <Link
                  to="/contact"
                  className="inline-flex items-center gap-2 rounded-full bg-white/10 px-7 py-3.5 text-sm font-medium text-white ring-1 ring-white/30 transition-colors hover:bg-white/20"
                >
                  <MapPin className="h-4 w-4" /> Find Us in Musaffah
                </Link>
              </div>
            </div>
          </div>
        </Reveal>
      </section>
    </SiteLayout>
  );
}
